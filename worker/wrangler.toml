name = "article-voice-proxy"
main = "cors-proxy.js"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGIN = "https://fabian20ro.github.io"

# PROXY_SECRET is set via: wrangler secret put PROXY_SECRET
# The GitHub Actions workflow passes it from the repository secret.
